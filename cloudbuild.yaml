steps:
  # 1️⃣ Instalar Node y dependencias
  - name: 'node:22.17-bullseye'
    id: 'Install dependencies'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "Installing dependencies..."
        npm ci

  # 2️⃣ Detectar apps afectadas
  - name: 'node:22.17-bullseye'
    id: 'Affected apps'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "Detecting affected projects..."
        AFFECTED_APPS=$(npx nx print-affected --target=build --base=origin/main~1 --head=HEAD --select=projects)
        echo "Detected apps: $AFFECTED_APPS"
        # Guardar la salida en un archivo temporal
        echo "$AFFECTED_APPS" > /workspace/affected_apps.txt

  # 3️⃣ Construir y desplegar solo apps afectadas
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build and deploy apps'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        # Leer apps afectadas del archivo
        AFFECTED_APPS=$(cat /workspace/affected_apps.txt)
        if [ -z "$AFFECTED_APPS" ]; then
          echo "No apps affected. Nothing to build."
          exit 0
        fi
        echo "Affected apps: $AFFECTED_APPS"
        for app in $AFFECTED_APPS; do
          echo "🚀 Building $app..."
          docker build -t gcr.io/$PROJECT_ID/$app ./apps/$app
          docker push gcr.io/$PROJECT_ID/$app
          echo "Deploying $app to Cloud Run..."
          gcloud run deploy $app \
            --image=gcr.io/$PROJECT_ID/$app \
            --platform=managed \
            --region=us-central1 \
            --quiet

# 4️⃣ Opciones generales de la build
options:
  logging: CLOUD_LOGGING_ONLY

# 5️⃣ Timeout opcional
timeout: "1800s"
